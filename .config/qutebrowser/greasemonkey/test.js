(function () {
  'use strict';

// ==UserScript==
// @name          Test
// @namespace     https://phenax.github.io
// @description   Testing
// @run-at        document-end
// ==/UserScript==


// const $ = document.querySelector.bind(document);
// const h = (el, props, children) => {
  // const $el = Object.assign(document.createElement(el), props);
  // children.forEach($ch => $el.appendChild($ch));
  // return $el;
// };
// const text = str => {
  // const $t = document.createTextNode(str);
  // return $t;
// };
// const render = ($el, $tree, append = false) => {
  // while(!append && $el.firstChild) $el.removeChild($el.firstChild);
  // $el.appendChild($tree);
// };

// const style = [
  // 'position: fixed',
  // 'top: 0',
  // 'right: 0',
  // 'z-index: 99999',
  // 'background: red',
  // 'padding: 5px',
// ].join(';') + ';';

// GM.xmlHttpRequest({
  // method: "GET",
  // url: "https://httpbin.org/get",
  // onload: function(response) {
    // console.log(response.responseText);
  // }
// });

// render(document.body, h('div', { style, onclick }, [
  // text('Hello qorld'),
// ]), true);


})();
